# Exploratory development (no issue yet)

1. Inspect request and repo state; treat as development-stage exploration.
2. Create a local task note or temporary work record if useful (optional).
3. Do not invent or reference a fake issue.
4. Implement narrowly; run tests; update docs if behavior or config changed.
5. Document discoveries that should become issues (e.g. "Recommend creating issue for X").
6. After the pass, propose formalizing: create an issue and link branch/PR if you open one.
7. Still produce a PR-ready summary if code changed.
